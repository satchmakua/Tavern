# /docs — Design notes and reference material

The canonical design lives at the repo root: [tavern-design.md](../tavern-design.md) (v2).

Use this directory for supporting material as it accumulates:

- AMAI internals — which globals each directive flips (the Hive Workshop "JASS Campaign AI 2.0" thread maps these)
- `war3_lua` setup notes + the known-good Reforged build to pin
- Bridge file-format specs (state file, directive file)
- Latency/throughput measurements per model tier
- Debugging logs and emergent-behavior observations
