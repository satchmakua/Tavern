# Runtime deps for the Companion Daemon (M1: chat-only, no game).
# Install:  python -m pip install -r requirements.txt
httpx>=0.27
pydantic>=2.6
pyyaml>=6.0

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
